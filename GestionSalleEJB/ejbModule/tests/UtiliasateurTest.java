package tests;


import metier.entities.Utilisateur;
import metier.utilisateur.UtilisateurImp;

public class UtiliasateurTest {
/*
    public static void main(String[] args) {
        try {
            // Manually instantiate the implementation (basic testing)
            UtilisateurImp utilisateurService = new UtilisateurImp();

            // Mock EntityManager (optional) - for now, avoid persistence operations
            System.out.println("Testing ajouterUtilisateur method...");

            // Create a user
            Utilisateur user = new Utilisateur("John Doe", "john.doe@example.com", "password123", "admin");
            utilisateurService.ajouterUtilisateur(user);
            System.out.println("User added: " + user.getNom());

            // Modify the user
            user.setNom("John Updated");
            utilisateurService.modifierUtilisateur(user);
            System.out.println("User updated: " + user.getNom());

        } catch (Exception e) {
            e.printStackTrace();
        }
    }*/
}

